# Variables d’environnement

**[Ouvrir `web/.env`](web/.env)** — place le curseur sur le lien puis **Ctrl+clic** (Windows) ou **Cmd+clic** (Mac).

Si le fichier n’existe pas encore : duplique [`web/.env.example`](web/.env.example) en le renommant `.env` dans le dossier `web`.
